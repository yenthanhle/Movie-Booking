<div class="container mt-4 customer-infor">
    <div class="left">
        <div class="btn-group-vertical mt-4">
            <a href="/customer/account" class="btn btn-outline-success mt-4">
                Thông tin chung
            </a>
            <a href="/customer/orders" class="btn btn-success mt-4">
                Hóa đơn
            </a>
        </div>
    </div>
    <div class="right">

        {{#if orders}}
        {{#each orders}}
        <div>
            <label>{{this._id}}</label>
            <a href="/customer/orders/{{this._id}}">Hoá đơn</a>
        </div>
        {{/each}}
        {{/if}}
        {{!-- <form method="POST" action="/customer/avatar/{{user._id}}/update" enctype="multipart/form-data">
            <div class="form-group">
                <label for="exampleFormControlFile1">Cập nhật ảnh đại diện</label>
                <input type="file" class="form-control-file" id="exampleFormControlFile1" name="avatar">
            </div>
            <a>
                <button type="submit" id="btnUpdateAva" class="btn btn-outline-success">Cập
                    nhật ava</button>
            </a>
        </form> --}}
        {{!-- <h2>Thông tin hoá đơn</h2>
        <div class="customer-infor">
            {{#if user}}
            <div class=" input-group input-group-sm mb-3 ">

                <div class=" form-field">
                    <label class="label">Mã hoá đơn</label>
                    <input readonly class="form-control input" id="text" value={{bill._id}}>
                </div>

                <div class="form-field">
                    <label class="label">Ngày tạo</label>
                    <input readonly class="form-control input" id="date_of_birth" name="date_of_birth" type="date"
                        value={{bill.time_created}}>
                </div> --}}
                {{!-- <div class="form-field">
                    <label class="label">Tên phim</label>
                    <input readonly class="form-control input" id="date_of_birth" name="date_of_birth" type="date"
                        value={{bill.date_of_birth}}>
                </div>
                <div class="form-field">
                    <label class="label">Ngày chiếu</label>
                    <input readonly class="form-control input" id="date_of_birth" name="date_of_birth" type="date"
                        value={{bill.date_of_birth}}>
                </div>
                <div class="form-field">
                    <label class="label">Tên rạp</label>
                    <input readonly class="form-control input" id="date_of_birth" name="date_of_birth" type="date"
                        value={{bill.date_of_birth}}>
                </div>
                <div class="form-field">
                    <label class="label">Phòng</label>
                    <input readonly class="form-control input" id="date_of_birth" name="date_of_birth" type="date"
                        value={{bill.date_of_birth}}>
                </div> --}}
                {{!-- <div class="form-field">
                    <label class="label">Tên ghế</label>
                    <input readonly class="form-control input" id="address" name="address" type="text"
                        value="{{bill.seat_name}}">
                </div>
                {{else}}
                <h2>ERROR!!!</h2>
            </div>
            {{/if}}
        </div> --}}
    </div>
</div>